# TP-CUST-132 Evidence: Screen Reader Labels Customer

- Existing forms use labelled `Input`, `Select`, `Textarea`, and `Checkbox` components.
- Muse chat uses `aria-label`, `aria-live="polite"`, labelled suggestions, and labelled input.
- Public, traveller, and companion shells now expose a consistent skip link and main landmark.
- Browser MCP snapshots loaded `/` and `/overview` successfully with route titles and no console warnings/errors.
